import React from 'react';
import styled from 'styled-components';

const Container = styled.section`
  height: 100vh;
  background-color: blue;
`;

const ProjectsPage = () => <Container>Projects page</Container>;

export default ProjectsPage;
